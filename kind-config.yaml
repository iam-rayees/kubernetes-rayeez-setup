kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # inside cluster
        hostPort: 8080         # exposed to your host
        protocol: TCP
  - role: worker
  - role: worker

